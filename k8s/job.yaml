apiVersion: batch/v1
kind: Job

metadata:
  name: hello-job

spec:
  template:
    spec:
      containers:
        - name: hello

          image: busybox:latest

          command:
            - /bin/sh
            - -c
            - echo "Hello Kubernetes Job"; sleep 10

      restartPolicy: Never

  backoffLimit: 3